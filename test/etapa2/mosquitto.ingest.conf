# Mosquitto — broker de INGESTAO (dispositivo Classe 0/1 -> RPi), Etapa 2.
# DEDICADO e distinto do broker do UNS. Local ao no, anonimo, sem persistencia.
# Topico-raiz: sida/ingest/{device_id}/{profile|telemetry}
listener 1883
allow_anonymous true
persistence false
log_type warning
log_type error
