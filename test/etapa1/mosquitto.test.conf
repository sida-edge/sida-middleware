# Mosquitto — broker LOCAL e ISOLADO para os testes da Etapa 1.
# Sem relacao com o broker de producao (GCP). Anonimo, sem persistencia.
listener 1883
allow_anonymous true
persistence false
log_type warning
log_type error
