# Isolated TEST broker only — unrelated to the production GCP Mosquitto.
# TCP listener for the Python publisher, WebSocket listener for the browser.
listener 1883
protocol mqtt

listener 9001
protocol websockets

allow_anonymous false
password_file /mosquitto/config/pwfile
